/**
 * プレミアム・課金まわりの定数。
 * 開発用フラグはここで一元管理し、本番で誤って有効にならないように __DEV__ と組み合わせる。
 */

/**
 * 開発中だけプレミアムを強制解除するフラグ。
 * - __DEV__ が true（開発ビルド）のときのみ true にできる。
 * - 本番ビルドでは __DEV__ が false のため、常に false になり課金状態で制御される。
 * デバッグ時に解除したいときは `__DEV__ && true` のまま、
 * 開発中でも課金UIを試したいときは `__DEV__ && false` に変更する。
 */
export const DEV_FORCE_PREMIUM_UNLOCK = __DEV__ && false;

